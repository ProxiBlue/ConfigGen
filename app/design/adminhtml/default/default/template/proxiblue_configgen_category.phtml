<?php
echo "<?php\n";
?>
/**
 * This file was generated by using ProxiBlue Config Generator
 * You can edit the generated config file by making changes in the template file
 * proxiblue_configgen_category.phtml located in the admin design folder.
 *
 * Copy this file to you extension data install folder, and adjust versions accordingly
 *
 * Latest code available from our website www.proxiblue.com.au
 *
 */

$installer = $this;
$installer->startSetup();

$configData = json_decode('<?php echo json_encode($this->getConfigItems());?>');

// create new category
$category = Mage::getModel('catalog/category');
$category->load($configData->entity_id);
if(!$category->getId()) {
unset($configData->entity_id);
}
foreach ($configData as $key => $value) {
$category->setData($key, $value);
}
$category->save();
$installer->endSetup();
